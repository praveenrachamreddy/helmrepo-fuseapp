# Default values for helmrepo-fuseapp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: image-registry.openshift-image-registry.svc:5000/fuse-pipelines/spring-boot-mongo-docker-git
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v2"

service:
  type: ClusterIP
  port: 8080

