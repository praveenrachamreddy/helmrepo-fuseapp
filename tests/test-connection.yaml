apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmrepo-fuseapp.fullname" . }}-test-connection"
  labels:
    {{- include "helmrepo-fuseapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmrepo-fuseapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
